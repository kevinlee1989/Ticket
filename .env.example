# Copy to .env for local scripts (never commit .env).
# Deployed Lambdas receive these via the SAM template instead.
OPENSEARCH_ENDPOINT=https://your-domain.us-west-2.aos.amazonaws.com
OPENSEARCH_USERNAME=admin
OPENSEARCH_PASSWORD=change-me
OPENSEARCH_EVENTS_INDEX=events
